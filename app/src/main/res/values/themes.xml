<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.RoadSafe" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.RoadSafe.Starting" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">@color/black</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>  # Required for
        # animated icons and only works for android 12 and above.

        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/Theme.RoadSafe</item>  # Required.
    </style>

</resources>